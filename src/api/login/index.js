const fetchState = require('./fetchstate');
const submitCallback = require('./submitcallback');
const submitCredentials = require('./submitcredentials');

module.exports = {
    fetchState,
    submitCallback,
    submitCredentials
};